{
	"name": "select",
	"description": "select",
	"repository": "scrapo/select",
	"version": "1.0.0",
	"main": "select.js",
	"scripts": {
		"test": "mocha --ui tdd test"
	},
	"dependencies": {
		"each": "scrapo/each",
		"flatten": "scrapo/flatten"
	},
	"devDependencies": {
		"mocha": "^3.1.2"
	}
}